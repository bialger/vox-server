# Forks cannot run on push to this repo; run the same checks on pull_request only when head is a fork.
name: "CI (fork PR)"

on:
  pull_request:
    branches: [master]

jobs:
  ci:
    if: github.event.pull_request.head.repo.fork
    uses: ./.github/workflows/ci-reusable.yml
    secrets: inherit
