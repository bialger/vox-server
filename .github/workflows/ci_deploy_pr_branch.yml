# Deploy when: (1) CI tests finished on a push to a non-master branch with an open PR to master,
# or (2) a PR to master was opened/reopened after CI already ran (push happened before the PR existed).
name: "Deploy PR branch"

on:
  workflow_run:
    workflows: ["CI tests"]
    types: [completed]
  pull_request:
    branches: [master]
    types: [opened, reopened, ready_for_review]

jobs:
  docker-publish:
    name: Build and push image (GHCR)
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'workflow_run' &&
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.head_branch != 'master') ||
      (github.event_name == 'pull_request' &&
        github.event.pull_request.base.ref == 'master' &&
        github.event.pull_request.head.repo.full_name == github.repository)
    permissions:
      contents: read
      actions: read
      packages: write
      pull-requests: read
    concurrency:
      group: docker-publish-${{ github.workflow }}
      cancel-in-progress: false
    outputs:
      deploy: ${{ steps.resolve.outputs.deploy }}
      sha: ${{ steps.resolve.outputs.sha }}
      branch: ${{ steps.resolve.outputs.branch }}
    steps:
      - name: Resolve when to deploy (workflow_run vs pull_request)
        id: resolve
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            if (context.eventName === 'workflow_run') {
              const run = context.payload.workflow_run;
              if (run.conclusion !== 'success' || run.event !== 'push' || run.head_branch === 'master') {
                core.setOutput('deploy', 'false');
                return;
              }
              const head = run.head_branch;
              const { data: prs } = await github.rest.pulls.list({
                owner,
                repo,
                state: 'open',
                base: 'master',
                head: `${owner}:${head}`,
              });
              if (prs.length === 0 || prs[0].draft) {
                core.setOutput('deploy', 'false');
                return;
              }
              core.setOutput('deploy', 'true');
              core.setOutput('sha', run.head_sha);
              core.setOutput('branch', head);
              core.setOutput('ci_run_id', String(run.id));
              return;
            }

            if (context.eventName === 'pull_request') {
              const pr = context.payload.pull_request;
              if (pr.base.ref !== 'master' || pr.head.repo.full_name !== `${owner}/${repo}`) {
                core.setOutput('deploy', 'false');
                return;
              }
              if (pr.draft) {
                core.info('Draft PR; skip deploy (use ready_for_review when marked ready).');
                core.setOutput('deploy', 'false');
                return;
              }
              const sha = pr.head.sha;
              const branch = pr.head.ref;

              const { data } = await github.request('GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs', {
                owner,
                repo,
                workflow_id: 'ci_tests.yml',
                branch: branch,
                event: 'push',
                status: 'completed',
                per_page: 50,
              });
              const wr = data.workflow_runs.find((r) => r.head_sha === sha && r.conclusion === 'success');
              if (!wr) {
                core.info(`No successful CI tests run for head_sha=${sha}; skip (push again or wait for CI).`);
                core.setOutput('deploy', 'false');
                return;
              }
              core.setOutput('deploy', 'true');
              core.setOutput('sha', sha);
              core.setOutput('branch', branch);
              core.setOutput('ci_run_id', String(wr.id));
              return;
            }

            core.setOutput('deploy', 'false');

      - name: Skip
        if: steps.resolve.outputs.deploy != 'true'
        run: echo "Skipping image and deploy."

      - uses: actions/checkout@v4
        if: steps.resolve.outputs.deploy == 'true'
        with:
          ref: ${{ steps.resolve.outputs.sha }}

      - name: Download vox-server from CI run
        if: steps.resolve.outputs.deploy == 'true'
        uses: actions/download-artifact@v4
        with:
          name: vox-server-linux
          path: docker-build-context
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          run-id: ${{ steps.resolve.outputs.ci_run_id }}

      - name: Prepare Docker build context
        if: steps.resolve.outputs.deploy == 'true'
        run: cp deploy/docker-entrypoint.sh docker-build-context/docker-entrypoint.sh

      - name: Image name (lowercase for GHCR)
        id: meta
        if: steps.resolve.outputs.deploy == 'true'
        run: |
          echo "repository_lower=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        if: steps.resolve.outputs.deploy == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (prebuilt binary, image tagged with commit SHA)
        if: steps.resolve.outputs.deploy == 'true'
        uses: docker/build-push-action@v6
        with:
          context: docker-build-context
          file: ./deploy/Dockerfile.prebuilt
          push: true
          tags: ghcr.io/${{ steps.meta.outputs.repository_lower }}:${{ steps.resolve.outputs.sha }}

      - name: Tag latest
        if: steps.resolve.outputs.deploy == 'true'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker pull "ghcr.io/${{ steps.meta.outputs.repository_lower }}:${{ steps.resolve.outputs.sha }}"
          docker tag "ghcr.io/${{ steps.meta.outputs.repository_lower }}:${{ steps.resolve.outputs.sha }}" \
            "ghcr.io/${{ steps.meta.outputs.repository_lower }}:latest"
          docker push "ghcr.io/${{ steps.meta.outputs.repository_lower }}:latest"

  deploy-server:
    name: Deploy (pull image + compose up)
    runs-on: ubuntu-latest
    needs: [docker-publish]
    if: >
      needs.docker-publish.result == 'success' &&
      needs.docker-publish.outputs.deploy == 'true'
    concurrency:
      group: deploy-production
      cancel-in-progress: false
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          GHCR_READ_TOKEN: ${{ secrets.GHCR_READ_TOKEN }}
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
          GITHUB_REPO_OWNER: ${{ github.repository_owner }}
        with:
          host: messenger.bialger.com
          username: ${{ secrets.SERVER_LOGIN }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          command_timeout: 30m
          envs: GHCR_READ_TOKEN,GHCR_USERNAME,GITHUB_REPO_OWNER
          script_stop: true
          script: |
            set -euo pipefail
            BRANCH="${{ needs.docker-publish.outputs.branch }}"
            REPO_LC=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
            SHA="${{ needs.docker-publish.outputs.sha }}"
            IMAGE="ghcr.io/${REPO_LC}:${SHA}"
            cd /opt/vox-server
            git fetch origin "${BRANCH}"
            git checkout "${BRANCH}"
            git pull --ff-only origin "${BRANCH}"
            cd deploy
            if [ ! -f .env ]; then
              printf '%s\n' "VOX_IMAGE=${IMAGE}" > .env
            elif grep -q '^VOX_IMAGE=' .env 2>/dev/null; then
              grep -v '^VOX_IMAGE=' .env > .env.tmp
              mv .env.tmp .env
              printf '%s\n' "VOX_IMAGE=${IMAGE}" >> .env
            else
              printf '%s\n' "VOX_IMAGE=${IMAGE}" >> .env
            fi
            if [ -n "${GHCR_READ_TOKEN:-}" ]; then
              U="${GHCR_USERNAME:-${GITHUB_REPO_OWNER}}"
              echo "${GHCR_READ_TOKEN}" | docker login ghcr.io -u "${U}" --password-stdin
            fi
            docker compose pull vox-server
            docker compose up -d
